spring:
  # DataSource Config
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://175.24.202.54:3306/user_center
    username: root
    password: root
  #session登录态的失效时间
  session:
    timeout: 86400

server:
  address: 0.0.0.0